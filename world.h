#pragma once

// Purely functional; this is a handler class, not a tangible in-game object

class World {
	public:
		// Add related visual functions (load chunks, clear chunks, sharpenWorld, shrink )
		// Remember to populate in the class .cpp
};